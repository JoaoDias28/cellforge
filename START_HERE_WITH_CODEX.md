# First Codex instruction

Open this repository at its root and execute `codex/tasks/TASK-001-repository-bootstrap.md`.

Before editing:

1. read `AGENTS.md`;
2. read `SYSTEM_SPEC.md`;
3. read the task;
4. inspect the repository;
5. create an ExecPlan because the task spans repository structure and CI.

Implement only Task 001. Run every acceptance check available in the environment. Where a ROS or Ubuntu-specific check cannot run, build the closest reproducible containerized check and report the limitation without claiming success.
