# cellforge_interfaces

This is the buildable ROS 2 Jazzy package boundary created by Task 001. It intentionally does
not generate messages, services, or actions yet. Task 007 will consume the canonical definitions
under the repository-root `ros_interfaces/` directory and add compatibility tests.
