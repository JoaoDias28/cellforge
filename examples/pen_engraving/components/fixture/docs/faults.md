# Reference pen fixture simulation faults

- `fixture.sensor.seating_failed`: the simulated product pose is outside the declared 0.5 mm
  translation or 1 degree angular seating tolerance, or seating fault injection is active.

The signal models fixture feedback for scenario testing. It is not a safety-rated input.
